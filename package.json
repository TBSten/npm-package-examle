{
  "name": "<<-package-name->>",
  "license": "MIT",
  "description": "<<-package-description->>",
  "keywords": [],
  "version": "0.0.0",
  "private": false,
  "repository": "<<-github-repository->>",
  "dependencies": {},
  "scripts": {
    "build": "<<-build-command->>",
    "prepublishOnly": "npm run build && cp -r ./src/* . && rm -rf ./lib",
    "postpublish": "git clean -fd"
  },
  "publishConfig": {
    "access": "public"
  }
}